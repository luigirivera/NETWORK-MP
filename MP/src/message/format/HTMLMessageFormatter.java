package message.format;

import message.Message;
import message.TextMessage;

public class HTMLMessageFormatter implements MessageFormatter {
	@Override
	public String format(Message<?> message) {
		//to-do: message with color
		return null;
	}
}